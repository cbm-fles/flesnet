#!/bin/bash

set -e
shopt -u nocaseglob
LANG=C

DIR="$(git rev-parse --show-toplevel)"
cd "$DIR"

clang-format-22 -style=file -fallback-style=none -i \
  lib/{cri,fles_core,fles_ipc,flib_ipc,logging,monitoring,pda,shm_ipc,tsb}/*.[hc]pp \
  app/*/*.[hc]pp \
  test/*.[hc]pp
